using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResultPanel2Init : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        ResultPanel2Controller.Instance.Init();
    }
}
